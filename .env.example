# Leave empty to keep the hosted interface in safe demonstration mode.
# Point this to the enterprise HTTPS backend to enable real PDF processing.
NEXT_PUBLIC_PROCESSING_API_BASE=http://localhost:8000
